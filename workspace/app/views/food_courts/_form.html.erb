<%= form_for(@food_court) do |f| %>
  <% if @food_court.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@food_court.errors.count, "error") %> prohibited this food_court from being saved:</h2>

      <ul>
      <% @food_court.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
