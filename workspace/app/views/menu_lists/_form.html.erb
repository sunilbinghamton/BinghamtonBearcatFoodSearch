<%= form_for(@menu_list) do |f| %>
  <% if @menu_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@menu_list.errors.count, "error") %> prohibited this menu_list from being saved:</h2>

      <ul>
      <% @menu_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
