<%= form_for(@food_outlet) do |f| %>
  <% if @food_outlet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@food_outlet.errors.count, "error") %> prohibited this food_outlet from being saved:</h2>

      <ul>
      <% @food_outlet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
